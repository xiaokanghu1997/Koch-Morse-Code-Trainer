import { Text } from "@fluentui/react-components";

export const AboutPage = () => {
  return (
    <div>
      <Text size={600} weight="semibold">About</Text>
      <Text>About page content to be added...</Text>
    </div>
  );
};