import { Text } from "@fluentui/react-components";

export const StatisticsPage = () => {
  return (
    <div>
      <Text size={600} weight="semibold">Statistics</Text>
      <Text>Statistics page content to be added...</Text>
    </div>
  );
};