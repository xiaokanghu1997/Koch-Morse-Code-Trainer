import { Text } from "@fluentui/react-components";

export const TrainingPage = () => {
  return (
    <div>
      <Text size={600} weight="semibold">Training</Text>
      <Text>Training page content to be added...</Text>
    </div>
  );
};