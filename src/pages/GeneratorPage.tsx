import { Text } from "@fluentui/react-components";

export const GeneratorPage = () => {
  return (
    <div>
      <Text size={600} weight="semibold">Generator</Text>
      <Text>Generator page content to be added...</Text>
    </div>
  );
};